# Supabase project (same project shared with the web app)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# URL of the deployed web app's OCR backend (/api/scan).
# Leave unset in local dev to auto-detect the Metro dev server's LAN IP.
EXPO_PUBLIC_API_URL=https://web-ten-sigma-26.vercel.app
